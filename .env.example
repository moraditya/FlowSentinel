# Backend
NIDS_MODE=demo
NIDS_API_KEY=dev-key-change-me
CORS_ORIGINS=http://localhost:3000,http://localhost:3001
# Leave unset to auto-detect (lo0 on macOS, lo on Linux)
# NIDS_CAPTURE_INTERFACE=

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:8000
# Server-side only (used by Next.js proxy route — never sent to client)
BACKEND_URL=http://localhost:8000
